# Countdown Tracker

Scaffolded Vite + React project. To finish setup locally you need Node.js and npm installed.

Commands:

```powershell
cd countdown-tracker
npm install
npm run dev
```

Notes:
- Requires Node.js >=14. Install from https://nodejs.org/.
- After `npm install` you can run the dev server with `npm run dev`.
